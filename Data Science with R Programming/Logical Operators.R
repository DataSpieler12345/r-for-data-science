x <- 100
y <- 200
 x == y
 
 #data
 wine.data <- read.csv("E:\\DATA SETS\\winequality-red.csv")
 # see data
 wine.data
 # see head data
 head(wine.data)
 
 # see again the data but in the panel as table format
 wine <- read.csv("E:\\DATA SETS\\winequality-red.csv")
View(wine)
